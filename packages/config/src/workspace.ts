/**
 * Workspace discovery surface used by `@laufen/cli`.
 *
 * Not part of the user-facing API — exposed as a sub-export for tooling
 * that needs to walk the workspace.
 */
export * from './workspace/index.ts';
